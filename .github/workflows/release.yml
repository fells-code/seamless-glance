name: Build & Release Seamless Glance

on:
  push:
    branches:
      - main
      - dev

permissions:
  contents: read

jobs:
  meta:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      is_prerelease: ${{ steps.version.outputs.is_prerelease }}
    steps:
      - uses: actions/checkout@v4

      - name: Extract version
        id: version
        run: |
          BASE_VERSION=$(grep '^version =' Cargo.toml | sed 's/version = "\(.*\)"/\1/')
          if [[ "${GITHUB_REF##*/}" == "dev" ]]; then
            VERSION="$BASE_VERSION-beta.${GITHUB_RUN_NUMBER}"
            PRERELEASE=true
          else
            VERSION="$BASE_VERSION"
            PRERELEASE=false
          fi

          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "is_prerelease=$PRERELEASE" >> $GITHUB_OUTPUT

          echo "Computed version: $VERSION"
  build-macos:
    runs-on: macos-latest
    needs: meta

    strategy:
      matrix:
        target:
          - aarch64-apple-darwin
          - x86_64-apple-darwin

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Extract version
        id: version
        run: |
          VERSION=$(grep '^version =' Cargo.toml | sed 's/version = "\(.*\)"/\1/')
          if [[ "${GITHUB_REF##*/}" == "dev" ]]; then
            VERSION="$VERSION-beta.${GITHUB_RUN_NUMBER}"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Cache cargo (macOS)
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: cargo-macos-${{ hashFiles('Cargo.lock') }}

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Rename binary
        run: |
          BIN=seamless-glance
          OUT=seamless-glance-${{ steps.version.outputs.version }}-${{ matrix.target }}
          mv target/${{ matrix.target }}/release/$BIN $OUT

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.target }}
          path: seamless-glance-*

  build-linux:
    runs-on: ubuntu-latest
    needs: meta

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu

      - name: Install deps
        run: sudo apt-get update && sudo apt-get install -y build-essential
      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: cargo-${{ runner.os }}-${{ hashFiles('Cargo.lock') }}
      - name: Build
        run: cargo build --profile release-linux --target x86_64-unknown-linux-gnu

      - name: Rename binary
        run: |
          BIN=seamless-glance
          OUT=seamless-glance-${{ needs.meta.outputs.version }}-x86_64-unknown-linux-gnu
          mv target/x86_64-unknown-linux-gnu/release-linux/$BIN $OUT

      - uses: actions/upload-artifact@v4
        with:
          name: linux
          path: seamless-glance-*

  publish:
    runs-on: ubuntu-latest
    needs: [build-macos, build-linux, meta]

    steps:
      - name: Checkout distro repo
        uses: actions/checkout@v4
        with:
          repository: fells-code/seamless-glance-distro
          token: ${{ secrets.SEAMLESS_GLANCE_GH_TOKEN }}

      - name: "Download build artifacts"
        uses: actions/download-artifact@v4

      - name: Publish release to distro repo
        uses: softprops/action-gh-release@v2
        with:
          repository: fells-code/seamless-glance-distro
          tag_name: v${{ needs.meta.outputs.version }}
          name: Seamless Glance v${{ needs.meta.outputs.version }}
          prerelease: ${{ needs.meta.outputs.is_prerelease }}
          files: |
            **/seamless-glance-*
          overwrite_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.SEAMLESS_GLANCE_GH_TOKEN }}
